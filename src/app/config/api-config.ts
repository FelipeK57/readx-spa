export const API_URL = import.meta.env.VITE_API_URL || "http://localhost:3000/api";

// Here you can add more endpoints as needed
export const ENDPOINT = {
  // Example endpoint
  USERS: `${API_URL}/users`,
};
